# このファイルを .env にコピーし、Azure SQL Database の接続情報を記入します。
# .env は .gitignore の対象です。リポジトリにコミットしないでください。

# 論理サーバー名（例: sql-iaas.database.windows.net）
DB_HOST=
# データベース名
DB_NAME=todo
# サーバー管理者ログインのユーザー名
DB_USER=
# サーバー管理者ログインのパスワード
DB_PASSWORD=
