# 待ち受けをループバックだけに限定します。設定しないと既定で全インターフェース
# （0.0.0.0）にバインドされ、認証のないアプリがローカルネットワークへ公開されます。
# 起動オプションではなく設定ファイルに書くことで、起動方法を誤っても露出しません。
[server]
address = "127.0.0.1"

# 捕捉していない例外のトレースバックを画面に出しません。
# ODBC のエラーメッセージにはサーバー名やユーザー名が含まれるためです（既定値は "full"）。
[client]
showErrorDetails = "none"
