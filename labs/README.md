# コース内ラボ

**セクションごとのハンズオン環境を Terraform で構築します。** 受講者は Azure Cloud Shell でコマンドを 1 行実行するだけで、演習の直前の状態から始められます。

> [!CAUTION]
> ラボは Udemy のコース内ラボ環境（ワークスペース）での実施を前提としています。
> ご自身の Azure サブスクリプションなどコース外の環境で実施すると、リソースの利用料金が発生します。
> コース外の環境で発生した課金について、講師は一切の責任を負いかねますのであらかじめご了承ください。

## ラボの一覧

| セクション | ラボ                                                               | 内容                                     |
| ---------- | ------------------------------------------------------------------ | ---------------------------------------- |
| 07         | [アプリを配置する前の環境を構築する](sections/section07/README.md) | VM と SQL を作ります。アプリは入れません |
| 09         | [Todo アプリの環境を構築する](sections/section09/README.md)        | VM・SQL・アプリをまとめて作ります        |

**カリキュラムの確定にあわせて追加します。** セクション名は確定後に加えます。

## 共通の実行方法

各ラボの手順書にあるコマンドを、Azure Cloud Shell（Bash）で実行します。

```bash
curl -fsSL https://raw.githubusercontent.com/m-oka-system/mssql-todo-app/refs/heads/main/<ラボのパス>/setup.sh | bash
```

### リソースグループの決まり方

**コース内ラボ環境では指定が不要です。** 用意されたリソースグループが 1 つだけのため、自動で検出します。

**ご自身のサブスクリプションで実行する場合は、事前に宣言してください。**

```bash
export TF_VAR_resource_group_name="rg-iaas"
```

**リソースグループは Terraform では作りません。** 片付けを「リソースグループごと削除する」に統一するためです。

**Cloud Shell はストレージなし（エフェメラル）で起動してください。** マウントしている場合、接続情報と秘密鍵、`terraform.tfstate` が `$HOME` に残り続けます。削除の手順は各ラボの「片付け」にあります。

## ディレクトリ構成

```text
labs/
├── README.md              # 本書
├── modules/               # 全ラボで共有する Terraform モジュール
│   ├── network/           # vnet・subnet・nsg をまとめて呼ぶ
│   └── <リソース名>/      # vnet・vm・mssql_server など、リソース単位
└── sections/
    └── sectionNN/         # コースのセクション番号がそのままラボのルートモジュール
        ├── README.md      # 手順書
        ├── setup.sh       # Cloud Shell から実行する入口
        └── *.tf           # Terraform
```

**1 つのラボを 1 つのディレクトリに閉じます。** 手順書と Terraform が同じ場所にあるため、片方だけが移動して対応が切れることがありません。

**モジュールは基本的にリソース単位です。** 例外は `network/` で、全ラボで内容が同じ `vnet`・`subnet`・`network_security_group` の呼び出しをまとめています。仮想マシンやデータベースはラボごとに設定が変わるため、まとめずにラボ側へ置きます。

**ディレクトリ名はコースのセクション番号だけにします。** 動画の番号とラボのパスが 1 対 1 で対応し、セクション名を変えてもパスが変わりません。

**state はラボごとに独立しています。** 他のラボを先に実行しなくても開始できます。

## 設計上の約束

| 約束                                          | 理由                                           |
| --------------------------------------------- | ---------------------------------------------- |
| **演習で教える対象は Terraform で作りません** | 受講者がポータルで作る体験がコースの主題です   |
| **リソースグループは作りません**              | 片付けをリソースグループの削除に統一します     |
| **バックエンドは設定しません**                | 片付けはリソースグループの削除で行うためです   |
| **プロバイダのバージョンは完全に固定します**  | 実行した日によって結果が変わらないようにします |
| **リソース名は動画の表示と一致させます**      | 受講者が画面と資料を照合できます               |

## 関連文書

| 文書                                               | 内容                             |
| -------------------------------------------------- | -------------------------------- |
| [Todo アプリの構築手順](../docs/build-todo-app.md) | ポータルから手作業で構築する手順 |
| [設計書](../docs/design.md)                        | 構成・ルーティング・通信仕様     |
| [トラブルシュート](../docs/troubleshooting.md)     | 症状別の対処                     |
